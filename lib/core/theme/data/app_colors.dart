import 'dart:ui';

import 'package:flutter/material.dart';

abstract class AppColors {
  static const primary = Color(0Xff37c0fe);
  static const List<Color> button = [Color(0Xff37c0fe), Color(0Xff0be7fe)];
  static const background = Color(0Xffe6edf5);
  static const darkBackground = Color(0xff081028);
  static const lightGrey = Color.fromARGB(255, 203, 211, 220);

  // static Color back(context) => Colors.red;
}
